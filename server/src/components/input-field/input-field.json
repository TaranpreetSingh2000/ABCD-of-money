{
  "collectionName": "components_input_field_input_fields",
  "info": {
    "displayName": "Input Field"
  },
  "options": {},
  "attributes": {
     "icon": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "images"
      ]
    },
    "inputFieldType": {
      "type": "enumeration",
      "required": true,
      "default": "text",
      "enum": [
        "text",
        "number",
        "email"
      ]
    },
    "inputFieldLabel": {
      "type": "string",
      "required": true,
      "regex": "^(?!\\s*$).+"
    },
    "inputFieldPlaceholder": {
      "type": "string",
      "required": true,
      "regex": "^(?!\\s*$).+"
    }
   
  },
  "config": {}
}
